import { describe, expect, test } from 'vitest'

describe('drawContainer', () => {
  test('should draw container', () => {})
  // TODO: each possible container outcomes
})

test('draw', () => {})

test('renderTo', () => {})

describe('begin', () => {
  test('should push parent', () => {})
  test('should push child', () => {})
})

test('dimension', () => {})

test('fill', () => {})

test('display', () => {})

test('border', () => {})

test('position', () => {})

test('text', () => {})
