import { describe, expect, test } from 'vitest'

describe('ensureOpenStack', () => {
  test('should error if no stack is open', () => {})
  test('should return the current stack', () => {})
})

describe('uStyle', () => {
  describe('position', () => {
    test('should set position single value', () => {})
    test('should set position x and y', () => {})
  })
  test('should set color', () => {})
  test('should set size', () => {})
  test('should set font', () => {})
  test('should begin', () => {})
})
